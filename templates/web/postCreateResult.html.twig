{% extends 'base.html.twig' %}

{% block title %}Create Post{% endblock %}

{% block body %}

    <div class="row align-items-md-stretch mb-5">
        <div class="col-md-12">
            <a href="{{ path('app_index') }}"><button class="btn btn-outline-secondary" type="button">Go back!</button></a>            
        </div>
    </div>

    <div class="row align-items-md-stretch">      
      <div class="col-md-12">

        {% if success  %}
          <h1><i class="bi bi-emoji-smile"></i> Saved</h1>
        {% else %}
          <h1><i class="bi bi-emoji-frown"></i> Error</h1>
        {% endif %}

        {{ result }}        
      </div>
    </div>

{% endblock %}
